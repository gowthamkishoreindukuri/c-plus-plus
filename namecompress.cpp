#include<bits/stdc++.h>
using namespace std;
int main()
{

    int n;
    cin>>n;
    while(n--)
    {
        string s;
        cin>>s;
        set<string> q;
        for(int i=0;i<s.size();i++)
        {
            for(int j=0;j<s.size();j*=(i+1))
            {
                for(int k=0;k<j;k++)
                {

                }
            }
        }
    }
}
